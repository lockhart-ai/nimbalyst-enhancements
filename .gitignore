# Dependencies
node_modules/

# Build output. Nimbalyst loads dist/index.js from this directory via a symlink
# in Application Support, so it must exist locally, but it is regenerated by
# `npm run build` and does not belong in history.
dist/

# OS / editor noise
.DS_Store
